# This VBA code executes the following:
* Creates a folder called **"TempFiles"** in the Local **"Downloads"** directory.
* If the folder already exists it **deletes** it and **creates** a new "TempFiles" folder.
