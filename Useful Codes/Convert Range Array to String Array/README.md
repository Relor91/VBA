# This VBA snippet executes the following:
* Converts all the elements of a **Range Array** into strings, making a **String Array**.
* In this way we can use the **String Array** as a criteria for **filtering**.
